# 工作日志

更新时间：2026-04-02 16:45:28 +0800

说明：本文档用于记录当前仓库协作过程中用户问过的问题，并给出简短结论。当前内容基于本轮可见对话整理，后续可持续追加。

## 一、Git、提交与推送

### 1. 问题：`怎么把这个分支推到远程仓库 https://github.com/Heminion/-`

结论：当前分支是 `develop-1`，远程 `origin` 已正确指向该仓库，标准推送命令是 `git push -u origin develop-1`。

### 2. 问题：提交编辑界面里如何填写 commit message

结论：可以在第一行直接写提交说明，例如“初始化项目上下文并更新面试复盘”；如果不想进入编辑器，更推荐直接使用 `git commit -m "提交说明"`。

## 二、Java 并发、锁与幂等

### 1. 问题：`【Q6】重试不会出现问题吗？你的每一步都是可重入的吗？为什么要问每一步是否可重入呢，可重入是指一个线程多次进入一个锁，那可重入有什么用呢，什么场景适合用可重入锁呢`

结论：面试语境里这里更应该理解为“重复执行是否安全、是否可重试、是否幂等”，不一定真在问 `ReentrantLock`；真正的可重入锁是指同一线程已拿到锁后还能再次进入同一把锁，主要用于嵌套调用、递归调用和带锁方法调用带锁方法的场景，避免同线程自我死锁。

### 2. 问题：`【Q10】用户同时点了两次会是什么情况？怎么保证幂等？幂等键：请求带唯一 requestId，先查是否已处理，有则直接返回成功。这个请求是来自哪里，前端吗？这个请求的requestId是什么`

结论：`requestId` 通常来自前端生成的 UUID 或服务端下发的防重令牌，用来标识“同一次业务提交”；在当前秒杀项目里，更核心的幂等保障是 Lua 中的一人一单校验、消费端再次查重，以及数据库唯一索引兜底。

### 3. 问题：`你能举个直观的例子详细讲讲可重入锁吗`

结论：已用 `withdraw()` 调用 `decrease()` 的例子解释可重入锁；核心理解是“同一线程已经拿到锁后，再次拿同一把锁不会把自己卡死”。

### 4. 问题：`既然withdraw都加过锁了，为什么decrease还要加锁呢`

结论：外层方法加锁是保护流程，内层方法加锁是保护该底层操作本身；如果内部方法希望可独立复用、自己保证线程安全，就可能也会加同一把锁，这时就依赖锁的可重入特性。

## 三、秒杀、消息队列与 Redis Stream

### 1. 问题：`【Q13】消息队列异步处理主要是做什么？解决什么问题？比你直接做有什么优势？这里的入队是什么，你能否定位一下原代码，这里的DB是什么，消费者是什么`

结论：当前项目里的“入队”是 `seckill.lua` 里执行 `XADD stream.orders` 把订单信息写入 Redis Stream；“DB” 指 MySQL 中的库存表和订单表；“消费者”是 `VoucherOrderServiceImpl` 中后台线程 `VoucherOrderHandler`。

### 2. 问题：`那DB是消费者按能力处理，假设我有两个人抢优惠券，优惠券只剩下一个，此时lua校验两个人都成功，db扣减库存时不久超卖了吗`

结论：不会；因为 Lua 脚本在 Redis 中原子串行执行，并且在入队前就先完成 Redis 库存判断和预扣减，库存只剩 1 时只能有一个请求成功入队，数据库层的 `stock > 0` 条件更新只是第二层兜底。

### 3. 问题：`【Q14】异步慢慢处理可能带来什么问题？用户已提示成功但消费失败怎么办？措施是什么？你能否详细说说 Redis Stream 的工作原理，最好提供图解`

结论：Redis Stream 是带消费者组和确认机制的消息流；消息被 `XREADGROUP` 读取后会进入 pending-list，只有 `XACK` 后才算真正消费完成，未确认消息可被重新拉取，因此适合异步下单这类需要失败恢复的场景。

### 4. 问题：`前端可轮询订单状态或通过 WebSocket 推送最终结果，请你详细说说这个过程是怎么实现的，以及什么是websocket`

结论：轮询是前端周期性调用订单状态查询接口，WebSocket 是前后端之间保持长连接的双向通信协议；在异步秒杀场景里，轮询实现简单，WebSocket 实时性更好且减少无效请求。

### 5. 问题：`【Q15】用消息队列能不能解决「扣金币和发钱在不同服务」？如果发钱永远不能成功怎么办？回滚的是什么？扣金币和发钱在不同服务是什么意思？`

结论：意思是两个业务动作分别由两个独立微服务执行，不在同一个本地事务里；消息队列只能帮助解耦、异步和重试，不能天然保证跨服务强一致，如果后一步长期失败，需要通过补偿事务回滚前一步已经成功的业务结果，例如把已扣掉的金币加回去。

## 四、面试文档与复盘理解

### 1. 问题：围绕 [2026-3-16腾讯后端1面复盘.md](/Users/kangk/Downloads/就业/面试项目/面试总结/2026-3-16腾讯后端1面复盘.md) 的多轮追问

结论：本轮对话重点集中在把秒杀、幂等、可重入锁、Redis Stream、异步处理、轮询、WebSocket、分布式补偿等概念讲清楚，目标是服务于腾讯后端一面复盘与面试表达。

## 五、分布式事务与一致性

### 1. 问题：`请你详细讲讲分布式事务`

结论：分布式事务是指一个业务操作跨多个服务或多个数据库时，如何保证整体结果一致；核心思路通常分为强一致方案和最终一致方案，面试中最常讲的是 2PC、TCC、可靠消息最终一致性与本地消息表/补偿机制。
